# P7 minimal length-four Milnor result

**Status:** completed under frozen preregistration

**Preregistration SHA-256:** `c013b36ff1d1c982c0531b061e8ce024b6db2aa3dbb7a2072bc25a75e74b1bb1`

**Frozen target:** `(R0, R1, R4, R5)`

**Outcome:** exact zero

## Result

The coefficient of `X_R0 X_R1 X_R4` in the degree-three exact
noncommutative Magnus expansion of the preferred longitude of `R5` is:

```text
mu_bar(R0,R1,R4,R5) = 0
```

The reverse-word coefficient is also zero. All four cyclic rotations of the
ordered quadruple return zero.

## Frozen gates

The evaluation passed every gate declared before outcome inspection:

- `[[x1,x2],x3]` reproduces the four required signed degree-three terms and no
  lower-degree terms;
- the exact parent P7 diagram is reused without projection changes;
- all six pairwise linking numbers of the target sublink are zero;
- all four length-three Milnor invariants are zero;
- every degree-one and degree-two term in the preferred longitude of `R5` is
  zero.

The result receipt records the ten crossing identifiers used by the extracted
sublink and preserves the parent diagram's computer-assisted standing.

## Frozen interpretation

The unique algebraically split four-component P7 sublink has no detected
length-four Milnor invariant under the frozen convention. Under the
preregistered Outcome B rule, ordinary length-four search does not escalate to
the maximal whole-link program. The symbolic Alexander ideal route has already
been completed independently; the remaining named discriminator is a higher
nilpotent quotient or a separately preregistered repeated-index/core-boundary
route.

## Boundary

This is not a complete ambient-isotopy classification, phase law,
prime-forcing result, spectral or zeta claim, or theorem-status escalation.
The crossing combinatorics remain computer-assisted with recorded nonzero
margins.
